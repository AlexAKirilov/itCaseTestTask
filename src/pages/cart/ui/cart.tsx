import React, { FC } from "react";
import {CartList} from "@/widgets/cart-list/ui/cart-list";

export const CartPage: FC = () => {
    return (
        <>
            <title>Тестовое задание | Корзина</title>
            <CartList />
        </>
    )
}
